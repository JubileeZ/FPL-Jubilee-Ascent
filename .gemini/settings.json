{
  "tools": {
    "allowed": [
      "run_shell_command(git status)",
      "run_shell_command(git log)",
      "run_shell_command(git diff)",
      "run_shell_command(ruff check src/)",
      "run_shell_command(uv run pytest tests/)",
      "run_shell_command(uv run python src/)"
    ]
  },
  "context": {
    "includeDirectories": ["docs/", "skills/"]
  }
}
